name: called workflow
on:
  workflow_call:
    inputs:
      info:
        required: true
        description: "info to be sent"
        type: string
jobs:
  called-job:
    runs-on: ubuntu-latest
    steps:
      - name: getting input from called worflow
        run:
          echo "the value is ${{ github.event.inputs.info }}"
      
